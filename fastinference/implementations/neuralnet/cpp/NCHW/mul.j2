{#
 # Mult / Scale layer
 #}

{% if scale.shape == (1,) %}
    for (int i = 0; i < {{ input_size }}; i++) {
        layer_{{ layer_id }}_output[i] = {{scale[0]}} * layer_{{ layer_id - 1 }}_output[i];
    }
{% else %}
 // TODO
{% endif %}