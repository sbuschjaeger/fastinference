{#
 # Binary Initialization
 #}
static constexpr {{ data_type }} layer_{{ layer_id }}_{{ name }}[{{ shape[:-1]|join('][') }}][{{ (shape[-1] / binary_word_size)|round(method='ceil')|int }}] = {{ value|replace('[', '{')|replace(']', '}')|replace('\'', '') }};
