{#
 # Binary Initialization
 #}
static constexpr {{ internal_type }} layer_{{ layer_id }}_{{ name }}[{{ shape[:-1]|join('][') }}][{{ (shape[-1] / backend.binary_word_size)|round(method='ceil')|int }}]{% if backend.align > 1 %} __attribute__((aligned({{ backend.align }}))){% endif %} = {{ value|replace('[', '{')|replace(']', '}')|replace('\'', '') }};
